{
  "name": "clinical-ops-workspace",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "engines": { "node": ">=20.19.0" },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/main.js && node --check api/auth.js && node --check api/workspace.js && node --check api/health.js && node --check lib/core.js"
  },
  "dependencies": {
    "postgres": "3.4.7",
    "xlsx": "0.18.5"
  },
  "devDependencies": { "vite": "7.1.7" }
}
