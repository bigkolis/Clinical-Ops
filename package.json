{
  "name": "clinical-ops-workspace",
  "private": true,
  "version": "0.9.1",
  "type": "module",
  "engines": { "node": ">=20.19.0" },
  "scripts": {
    "assemble": "node scripts/assemble.mjs",
    "predev": "npm run assemble",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "npm run assemble",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "npm run assemble && node --check src/app2.js && node --check src/login-polish.js && node --check src/clinical.js && node --check src/labels.js && node --check src/reports.js && node --check api/auth.js && node --check api/workspace.js && node --check api/snapshot.js && node --check api/operator.js && node --check api/health.js && node --check lib/core.js"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "jspdf": "2.5.2",
    "postgres": "3.4.7",
    "xlsx": "0.18.5"
  },
  "devDependencies": { "vite": "7.1.7" }
}
